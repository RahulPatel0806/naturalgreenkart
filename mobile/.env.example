# Mobile app environment (consumed via app.json `extra` / expo-constants).
# For physical devices use your machine's LAN IP, e.g. http://192.168.1.5:4000/api
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000/api
